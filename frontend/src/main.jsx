// main.jsx — Entry point: React render vào DOM
// BrowserRouter phải bọc App vì AuthProvider dùng useNavigate() — hook của React Router

import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App.jsx';
import './index.css';

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>
);
